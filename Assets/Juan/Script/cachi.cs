using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cachi : MonoBehaviour
{
    // Start is called before the first frame update
    public int sabor;
    public bool act=true;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
